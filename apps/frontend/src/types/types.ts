// TODO: Extract to separate file
type Friend = {
    id: string,
    firstName: string,
    lastName: string,
    image: string,
  }

  type User = {
    id: string,
    firstName: string,
    lastName: string,
    image: string,
  }

export type { Friend };
export type { User };
